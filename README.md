# Homelab Index

Next.js 16 App Router project for a self-hosted homelab dashboard.

## Prototype

Open `prototype/index.html` to review the visual direction. The prototype is separate from the empty application in `src/app`.

## Development

```bash
pnpm dev
```
